flows:
  - type: playground
    id: BUYER_SIDE_ORDER_CANCELLATION
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point cancellation flow
    config:
      $ref: ./Logistics (P2P)/BUYER_SIDE_ORDER_CANCELLATION.yaml
  - type: playground
    id: CANCELLATION_TERMS_FLOW
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: This Flow outlines the process for confirming and updating
      logistics orders with the Logistics Service Provider (LSP) and the
      Logistics Buyer Network Partner (LBNP). Here the LSP provides cancellation
      terms which would be applicable in case of a cancellation.
    config:
      $ref: ./Logistics (P2P)/CANCELLATION_TERMS_FLOW.yaml
  - type: playground
    id: CASH_ON_DELIVERY_FLOW
    usecase: Logistics (P2P)
    tags:
      - PRAMAAN
      - MANDATORY
      - REPORTABLE
      - WORKBENCH
    description: This flow defines the logistics process for Cash on Delivery (COD)
      retail orders, ensuring proper tracking and settlement between Logistics
      Buyer Network Participants (LBNPs) and Logistics Service Providers (LSPs).
    config:
      $ref: ./Logistics (P2P)/CASH_ON_DELIVERY_FLOW.yaml
  - type: playground
    id: Call-Masking
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point delivery flow including baseline features
    config:
      $ref: ./Logistics (P2P)/Call-Masking.yaml
  - type: playground
    id: E-POD
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point delivery flow including baseline features
    config:
      $ref: ./Logistics (P2P)/E-POD.yaml
  - type: playground
    id: E-WAY_BILL
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point delivery flow including baseline features
    config:
      $ref: ./Logistics (P2P)/E-WAY_BILL.yaml
  - type: playground
    id: FEATURE_DISCOVERY
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Feature discovery for point-to-point logistics operations
    config:
      $ref: ./Logistics (P2P)/FEATURE_DISCOVERY.yaml
  - type: playground
    id: FIFO_FLOW
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: The Immediate Delivery Flow outlines the process for confirming and
      updating logistics orders with the Logistics Service Provider (LSP) and
      the Logistics Buyer Network Partner (LBNP). This flow is mainly for F&B
      and Grocery orders. Ship to delivery (S2D) TAT for the same is less than
      60 minutes.
    config:
      $ref: ./Logistics (P2P)/FIFO_FLOW.yaml
  - type: playground
    id: IMMEDIATE_DELIVERY_FLOW
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: The Immediate Delivery Flow outlines the process for confirming and
      updating logistics orders with the Logistics Service Provider (LSP) and
      the Logistics Buyer Network Partner (LBNP). This flow is mainly for F&B
      and Grocery orders. Ship to delivery (S2D) TAT for the same is less than
      60 minutes.
    config:
      $ref: ./Logistics (P2P)/IMMEDIATE_DELIVERY_FLOW.yaml
  - type: playground
    id: ORDER_FLOW_BASE_LINE
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point delivery flow including baseline features
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_BASE_LINE.yaml
  - type: playground
    id: ORDER_FLOW_BASE_LINE_PUBLIC_SPECIAL_CAPABILITIES
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point delivery flow including baseline features
      with public special capabilities
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_BASE_LINE_PUBLIC_SPECIAL_CAPABILITIES.yaml
  - type: playground
    id: ORDER_FLOW_BASE_LINE_SELLER_CREDS
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point delivery flow including baseline features
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_BASE_LINE_SELLER_CREDS.yaml
  - type: playground
    id: ORDER_FLOW_BASE_LINE_SLA_METRICS
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point delivery flow including baseline features
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_BASE_LINE_SLA_METRICS.yaml
  - type: playground
    id: ORDER_FLOW_BASE_LINE_WITH_IGM(v-1.0.0)
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point delivery flow including baseline features
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_BASE_LINE_WITH_IGM_v-1_0_0_.yaml
  - type: playground
    id: ORDER_FLOW_BASE_LINE_WITH_IGM(v-2.0.0)
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point delivery flow including baseline features
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_BASE_LINE_WITH_IGM_v-2_0_0_.yaml
  - type: playground
    id: ORDER_FLOW_BASE_LINE_WITH_IGM_NO_ACTION(v-2.0.0)
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point delivery flow including baseline features
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_BASE_LINE_WITH_IGM_NO_ACTION_v-2_0_0_.yaml
  - type: playground
    id: ORDER_FLOW_BASE_LINE_WITH_IGM_REJECTION(v-2.0.0)
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point delivery flow including baseline features
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_BASE_LINE_WITH_IGM_REJECTION_v-2_0_0_.yaml
  - type: playground
    id: ORDER_FLOW_CODIFIED_STATIC_TERMS
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point delivery flow including baseline features
      with codifed static terms
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_CODIFIED_STATIC_TERMS.yaml
  - type: playground
    id: ORDER_FLOW_EXCHANGE_CUSTOMER_SUPPORT_DETAILS
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point delivery flow including baseline features
      with customer support details
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_EXCHANGE_CUSTOMER_SUPPORT_DETAILS.yaml
  - type: playground
    id: ORDER_FLOW_RCM
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point delivery flow including baseline features
      with rcm self-declartion
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_RCM.yaml
  - type: playground
    id: PREPAID_PAYMENT_FLOW
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: This flow outlines the Payment Wallet process for Logistics Service
      Providers (LSPs), enabling them to establish a prepaid wallet system for
      logistics buyers.Payments for services are deducted directly from this
      wallet, ensuring a seamless and efficient transaction process.
    config:
      $ref: ./Logistics (P2P)/PREPAID_PAYMENT_FLOW.yaml
  - type: playground
    id: QUICK_COMMERCE_FLOW
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: This flow covers logistics for Quick Commerce, Milk Run, and SPMD
      models, enabling coordination between LBNPs and LSPs.
    config:
      $ref: ./Logistics (P2P)/QUICK_COMMERCE_FLOW.yaml
  - type: playground
    id: RATE_CARD (P2P)
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Rate Card for point-to-point logistics operations
    config:
      $ref: ./Logistics (P2P)/RATE_CARD__P2P_.yaml
  - type: playground
    id: RTO_FLOW
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Flow for Return to Origin (RTO) when buyer is unavailable or
      delivery fails. A separate RTO fulfillment is created and linked to the
      forward shipment. RTO quote is added before RTO completes. RTO ends with
      Delivered or Disposed, completing the order.
    config:
      $ref: ./Logistics (P2P)/RTO_FLOW.yaml
  - type: playground
    id: SELLER_BUYER_INSTRUCTIONS
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point delivery flow including baseline features
    config:
      $ref: ./Logistics (P2P)/SELLER_BUYER_INSTRUCTIONS.yaml
  - type: playground
    id: SELLER_SIDE_ORDER_CANCELLATION
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point cancellation flow
    config:
      $ref: ./Logistics (P2P)/SELLER_SIDE_ORDER_CANCELLATION.yaml
  - type: playground
    id: STATIC_OTP_PICKUP_DELIVERY
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: This Flow outlines the process for confirming and updating
      logistics orders with the Logistics Service Provider (LSP) and the
      Logistics Buyer Network Partner (LBNP). Here the LBNP provides static OTP
      for pickup and delivery verification.
    config:
      $ref: ./Logistics (P2P)/STATIC_OTP_PICKUP_DELIVERY.yaml
  - type: playground
    id: STATIC_OTP_RTO_DELIVERY
    usecase: Logistics (P2P)
    tags:
      - PRAMAAN
      - MANDATORY
      - REPORTABLE
      - WORKBENCH
    description: Flow for Return to Origin (RTO) when buyer is unavailable or
      delivery fails. A separate RTO fulfillment is created and linked to the
      forward shipment. RTO quote is added before RTO completes. RTO ends with
      Delivered or Disposed, completing the order. Here LBNP provides static OTP
      for RTO delivery verification.
    config:
      $ref: ./Logistics (P2P)/STATIC_OTP_RTO_DELIVERY.yaml
  - type: playground
    id: SURGE_FEE_FLOW
    usecase: Logistics (P2P)
    tags:
      - PRAMAAN
      - MANDATORY
      - REPORTABLE
      - WORKBENCH
    description: This Flow outlines the process for confirming and updating
      logistics orders with the Logistics Service Provider (LSP) and the
      Logistics Buyer Network Partner (LBNP). This flow is mainly for F&B and
      Grocery orders. Here the LSP adds an additional surge charge due to any
      operational reason.
    config:
      $ref: ./Logistics (P2P)/SURGE_FEE_FLOW.yaml
  - type: playground
    id: UPDATE_DELIVERY_ADDRESS
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Standard point-to-point delivery flow including baseline features
    config:
      $ref: ./Logistics (P2P)/UPDATE_DELIVERY_ADDRESS.yaml
