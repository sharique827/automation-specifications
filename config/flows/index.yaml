flows:
  - type: playground
    id: BUYER_SIDE_ORDER_CANCELLATION
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Enables buyers to discover, book, and cancel peer-to-peer logistics services through a complete transaction lifecycle from search to cancellation confirmation.
    config:
      $ref: ./Logistics (P2P)/BUYER_SIDE_ORDER_CANCELLATION.yaml
  - type: playground
    id: CANCELLATION_TERMS_FLOW
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A peer-to-peer logistics transaction where a buyer searches for, confirms, and tracks a shipment order with a logistics service provider who establishes cancellation terms upfront. The flow captures the complete lifecycle from order discovery through confirmation, execution, and delivery with defined cancellation conditions.
    config:
      $ref: ./Logistics (P2P)/CANCELLATION_TERMS_FLOW.yaml
  - type: playground
    id: CASH_ON_DELIVERY_FLOW
    usecase: Logistics (P2P)
    tags:
      - PRAMAAN
      - MANDATORY
      - REPORTABLE
      - WORKBENCH
    description: This flow manages end-to-end cash-on-delivery logistics operations, enabling shippers to discover and book services, track shipments through real-time status updates, and facilitate payment settlement upon delivery between network participants.
    config:
      $ref: ./Logistics (P2P)/CASH_ON_DELIVERY_FLOW.yaml
  - type: playground
    id: Call-Masking
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: End-to-end point-to-point parcel delivery flow with real-time tracking and privacy-protected communication via call masking between shippers and logistics providers.
    config:
      $ref: ./Logistics (P2P)/Call-Masking.yaml
  - type: playground
    id: E-POD
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: End-to-end peer-to-peer delivery workflow from service discovery through shipment confirmation, real-time in-transit tracking with status updates, and electronic proof of delivery.
    config:
      $ref: ./Logistics (P2P)/E-POD.yaml
  - type: playground
    id: E-WAY_BILL
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Complete point-to-point logistics flow enabling shippers to discover providers, confirm shipments with e-way bill documentation, and track delivery progress through real-time status updates.
    config:
      $ref: ./Logistics (P2P)/E-WAY_BILL.yaml
  - type: playground
    id: FEATURE_DISCOVERY
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A shipper searches for logistics services to move goods between origin and destination points. Service providers respond with available transportation options and delivery capabilities for point-to-point movement.
    config:
      $ref: ./Logistics (P2P)/FEATURE_DISCOVERY.yaml
  - type: playground
    id: FIFO_FLOW
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A peer-to-peer immediate delivery logistics transaction flow enabling buyers to discover, confirm, and track time-sensitive orders with real-time status updates throughout fulfillment, supported by a sub-60-minute service level agreement.
    config:
      $ref: ./Logistics (P2P)/FIFO_FLOW.yaml
  - type: playground
    id: IMMEDIATE_DELIVERY_FLOW
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: This flow standardizes the immediate delivery logistics transaction for food and grocery orders through the ONDC network, enabling buyers to discover services and place orders while logistics providers confirm, track, and complete same-day delivery within 60 minutes. It encompasses the full peer-to-peer coordination lifecycle from initial search through delivery completion.
    config:
      $ref: ./Logistics (P2P)/IMMEDIATE_DELIVERY_FLOW.yaml
  - type: playground
    id: ORDER_FLOW_BASE_LINE
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: End-to-end point-to-point delivery transaction enabling customers to discover logistics services, place and confirm shipments, and track delivery status through real-time updates. This baseline flow exercises the complete order lifecycle from service search through shipment confirmation and continuous status monitoring.
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_BASE_LINE.yaml
  - type: playground
    id: ORDER_FLOW_BASE_LINE_PUBLIC_SPECIAL_CAPABILITIES
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Point-to-point logistics transaction enabling shippers to discover available delivery services, book and confirm shipments, and track deliveries with real-time status monitoring throughout the order lifecycle. The flow includes baseline mandatory operational capabilities and publicly accessible special handling features for enhanced service options.
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_BASE_LINE_PUBLIC_SPECIAL_CAPABILITIES.yaml
  - type: playground
    id: ORDER_FLOW_BASE_LINE_SELLER_CREDS
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Baseline point-to-point logistics transaction covering the complete order fulfillment journey from buyer discovery through seller confirmation, order status updates, and shipment tracking with seller authentication.
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_BASE_LINE_SELLER_CREDS.yaml
  - type: playground
    id: ORDER_FLOW_BASE_LINE_SLA_METRICS
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A complete point-to-point delivery transaction flow that establishes baseline SLA metrics across the customer journey from service discovery through order placement, confirmation, fulfillment tracking, and real-time status monitoring.
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_BASE_LINE_SLA_METRICS.yaml
  - type: playground
    id: ORDER_FLOW_BASE_LINE_WITH_IGM(v-1.0.0)
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Complete point-to-point logistics transaction flow covering service discovery, order fulfillment, shipment tracking, and integrated grievance management.
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_BASE_LINE_WITH_IGM_v-1_0_0_.yaml
  - type: playground
    id: ORDER_FLOW_BASE_LINE_WITH_IGM(v-2.0.0)
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: End-to-end peer-to-peer logistics transaction flow covering order confirmation, fulfillment tracking, and integrated issue grievance management across the complete delivery lifecycle.
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_BASE_LINE_WITH_IGM_v-2_0_0_.yaml
  - type: playground
    id: ORDER_FLOW_BASE_LINE_WITH_IGM_NO_ACTION(v-2.0.0)
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A complete point-to-point delivery operation validating end-to-end logistics from initial service search through order confirmation, real-time shipment tracking, and issue escalation. This baseline flow exercises the core delivery lifecycle including service discovery, order placement, tracking visibility, and issue notification without remedial action.
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_BASE_LINE_WITH_IGM_NO_ACTION_v-2_0_0_.yaml
  - type: playground
    id: ORDER_FLOW_BASE_LINE_WITH_IGM_REJECTION(v-2.0.0)
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Baseline point-to-point logistics delivery order executing a complete fulfillment lifecycle, with integrated issue grievance management and rejection handling scenarios.
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_BASE_LINE_WITH_IGM_REJECTION_v-2_0_0_.yaml
  - type: playground
    id: ORDER_FLOW_CODIFIED_STATIC_TERMS
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A point-to-point logistics transaction that enables a shipper to discover available delivery services, agree to codified service terms, and complete an order with real-time tracking through delivery completion.
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_CODIFIED_STATIC_TERMS.yaml
  - type: playground
    id: ORDER_FLOW_EXCHANGE_CUSTOMER_SUPPORT_DETAILS
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: An end-to-end point-to-point delivery order flow that enables service discovery, order confirmation, real-time tracking, and seamless integration of customer support details throughout the logistics transaction.
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_EXCHANGE_CUSTOMER_SUPPORT_DETAILS.yaml
  - type: playground
    id: ORDER_FLOW_RCM
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Complete lifecycle for point-to-point deliveries, enabling customers to search and book services, track shipments in real-time, and ensure reverse charge mechanism compliance throughout the transaction.
    config:
      $ref: ./Logistics (P2P)/ORDER_FLOW_RCM.yaml
  - type: playground
    id: PREPAID_PAYMENT_FLOW
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: This prepaid payment wallet flow enables logistics buyers to prepay for services with logistics providers and track service execution in real-time from order initiation through completion.
    config:
      $ref: ./Logistics (P2P)/PREPAID_PAYMENT_FLOW.yaml
  - type: playground
    id: QUICK_COMMERCE_FLOW
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: This flow orchestrates end-to-end quick commerce logistics by enabling shippers to discover and confirm on-demand delivery services, then tracks shipments through multiple status checkpoints as the parcel moves through the fulfillment network.
    config:
      $ref: ./Logistics (P2P)/QUICK_COMMERCE_FLOW.yaml
  - type: playground
    id: RATE_CARD (P2P)
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Shippers search for and discover point-to-point logistics rate options from transport providers to obtain pricing for direct shipments.
    config:
      $ref: ./Logistics (P2P)/RATE_CARD__P2P_.yaml
  - type: playground
    id: RTO_FLOW
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Manages Return to Origin (RTO) for failed or unavailable deliveries by creating a linked reverse logistics fulfillment, generating return freight quotes, and completing the transaction through return to sender or disposal.
    config:
      $ref: ./Logistics (P2P)/RTO_FLOW.yaml
  - type: playground
    id: SELLER_BUYER_INSTRUCTIONS
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A point-to-point delivery transaction where a buyer discovers and books logistics services, with the seller providing real-time coordination, tracking, and status updates throughout the fulfillment lifecycle.
    config:
      $ref: ./Logistics (P2P)/SELLER_BUYER_INSTRUCTIONS.yaml
  - type: playground
    id: SELLER_SIDE_ORDER_CANCELLATION
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Seller-initiated cancellation of a confirmed order in point-to-point logistics, encompassing the full transaction lifecycle from search and confirmation through cancellation with status updates.
    config:
      $ref: ./Logistics (P2P)/SELLER_SIDE_ORDER_CANCELLATION.yaml
  - type: playground
    id: STATIC_OTP_PICKUP_DELIVERY
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A peer-to-peer logistics flow enabling buyers to request, confirm, and track shipments with static OTP authentication for secure pickup and delivery verification. The flow supports order initialization, real-time status monitoring, and tracking throughout the complete shipment lifecycle.
    config:
      $ref: ./Logistics (P2P)/STATIC_OTP_PICKUP_DELIVERY.yaml
  - type: playground
    id: STATIC_OTP_RTO_DELIVERY
    usecase: Logistics (P2P)
    tags:
      - PRAMAAN
      - MANDATORY
      - REPORTABLE
      - WORKBENCH
    description: A return-to-origin (RTO) logistics flow that handles failed or refused deliveries by enabling secure return shipment verification through static OTP authentication. The RTO is managed as a linked fulfillment to the original order, with outcomes including return delivery confirmation or disposal.
    config:
      $ref: ./Logistics (P2P)/STATIC_OTP_RTO_DELIVERY.yaml
  - type: playground
    id: SURGE_FEE_FLOW
    usecase: Logistics (P2P)
    tags:
      - PRAMAAN
      - MANDATORY
      - REPORTABLE
      - WORKBENCH
    description: This flow represents a peer-to-peer logistics transaction where a buyer confirms a delivery order and the Logistics Service Provider applies a surge charge adjustment during fulfillment due to operational constraints, with both parties synchronized through status updates and tracking. The flow ensures the charge modification is communicated to and acknowledged by both the service provider and the buyer's network partner before final execution.
    config:
      $ref: ./Logistics (P2P)/SURGE_FEE_FLOW.yaml
  - type: playground
    id: UPDATE_DELIVERY_ADDRESS
    usecase: Logistics (P2P)
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Validates a point-to-point logistics ordering flow where shippers can search for carriers, confirm orders, and update delivery addresses prior to fulfillment. The scenario exercises end-to-end shipment visibility through continuous tracking and progressive status notifications.
    config:
      $ref: ./Logistics (P2P)/UPDATE_DELIVERY_ADDRESS.yaml
