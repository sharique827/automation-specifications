flows:
  - type: playground
    id: IntraCity_Merchant_Side_Cancellation
    usecase: Bus
    tags:
      - PRAMAAN
      - MANDATORY
      - REPORTABLE
      - WORKBENCH
    description: Exercises the complete intra-city bus booking lifecycle from passenger discovery and service selection through booking confirmation, validating the bus operator's ability to confirm and subsequently cancel confirmed reservations. This flow demonstrates the merchant's cancellation authority over confirmed bookings in the bus transport domain.
    config:
      $ref: ./Bus/IntraCity_Merchant_Side_Cancellation.yaml
  - type: playground
    id: IntraCity_Monthly_Passes_Flow_Code_Based
    usecase: Bus
    tags:
      - PRAMAAN
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Enables commuters to search, select, and purchase a monthly unlimited intra-city bus pass with code-based digital access for seamless transit entry.
    config:
      $ref: ./Bus/IntraCity_Monthly_Passes_Flow_Code_Based.yaml
  - type: playground
    id: IntraCity_Purchase_Journey_FlowCode_Based_Flow_With_IGM(v-1.0.0)
    usecase: Bus
    tags:
      - MANDATORY
      - WORKBENCH
    description: End-to-end intra-city bus ticket purchase and issue management flow, spanning passenger search, fare discovery, seat selection, booking confirmation, order tracking, and post-purchase grievance resolution.
    config:
      $ref: ./Bus/IntraCity_Purchase_Journey_FlowCode_Based_Flow_With_IGM_v-1_0_0_.yaml
  - type: playground
    id: IntraCity_Purchase_Journey_FlowCode_Based_Flow_With_IGM(v-2.0.0)
    usecase: Bus
    tags:
      - MANDATORY
      - WORKBENCH
    description: Complete intra-city bus ticket booking journey combining service search and selection, booking confirmation, and integrated issue resolution and grievance management for end-to-end customer support.
    config:
      $ref: ./Bus/IntraCity_Purchase_Journey_FlowCode_Based_Flow_With_IGM_v-2_0_0_.yaml
  - type: playground
    id: IntraCity_Purchase_Journey_FlowCode_Based_Flow_With_IGM_No_Action(v-2.0.0)
    usecase: Bus
    tags:
      - MANDATORY
      - WORKBENCH
    description: Intra-city bus ticket booking journey covering service discovery, selection, order confirmation, and integrated grievance management for post-purchase customer issues.
    config:
      $ref: ./Bus/IntraCity_Purchase_Journey_FlowCode_Based_Flow_With_IGM_No_Action_v-2_0_0_.yaml
  - type: playground
    id: IntraCity_Purchase_Journey_FlowCode_Based_Flow_With_IGM_Rejection(v-2.0.0)
    usecase: Bus
    tags:
      - MANDATORY
      - WORKBENCH
    description: End-to-end intra-city bus ticket purchase experience, from search and selection through booking confirmation, including comprehensive issue and grievance management with rejection handling.
    config:
      $ref: ./Bus/IntraCity_Purchase_Journey_FlowCode_Based_Flow_With_IGM_Rejection_v-2_0_0_.yaml
  - type: playground
    id: IntraCity_Purchase_Journey_Flow_Code_Based
    usecase: Bus
    tags:
      - PRAMAAN
      - MANDATORY
      - REPORTABLE
      - WORKBENCH
    description: Facilitates complete intra-city bus ticket purchasing, enabling travelers to discover routes, confirm bookings, and monitor orders in real-time.
    config:
      $ref: ./Bus/IntraCity_Purchase_Journey_Flow_Code_Based.yaml
  - type: playground
    id: IntraCity_Search_Pagination_FlowCode_Based
    usecase: Bus
    tags:
      - WORKBENCH
      - OPTIONAL
    description: A buyer searches for available bus services within a city, with the seller returning matching results in paginated batches through flow-code-based navigation.
    config:
      $ref: ./Bus/IntraCity_Search_Pagination_FlowCode_Based.yaml
  - type: playground
    id: IntraCity_Technical_Cancellation_Flow
    usecase: Bus
    tags:
      - PRAMAAN
      - MANDATORY
      - REPORTABLE
      - WORKBENCH
    description: End-to-end intra-city bus ticket booking and technical cancellation flow, demonstrating the complete customer journey from service discovery and selection through confirmation and post-booking cancellation processing.
    config:
      $ref: ./Bus/IntraCity_Technical_Cancellation_Flow.yaml
  - type: playground
    id: IntraCity_User_Based_Confirmation_flow
    usecase: Bus
    tags:
      - WORKBENCH
      - OPTIONAL
    description: An intra-city bus reservation flow where passengers search available routes, select a preferred service, and complete their booking through an explicit user confirmation before final transaction settlement.
    config:
      $ref: ./Bus/IntraCity_User_Based_Confirmation_flow.yaml
  - type: playground
    id: IntraCity_User_Cancellation_Flow
    usecase: Bus
    tags:
      - PRAMAAN
      - MANDATORY
      - REPORTABLE
      - WORKBENCH
    description: Validates the complete lifecycle of an intra-city bus ticket from passenger search and booking confirmation through cancellation of the confirmed reservation.
    config:
      $ref: ./Bus/IntraCity_User_Cancellation_Flow.yaml
  - type: playground
    id: IntraCity_Vehicle_Based_Confirmation_flow(With Update Call)
    usecase: Bus
    tags:
      - WORKBENCH
      - OPTIONAL
    description: An intra-city bus booking flow enabling passengers to search for available services, select a preferred bus, confirm their reservation, and update booking details as needed before completion.
    config:
      $ref: ./Bus/IntraCity_Vehicle_Based_Confirmation_flow_With_Update_Call_.yaml
  - type: playground
    id: IntraCity_Vehicle_Based_Confirmation_flow(Without Update Call)
    usecase: Bus
    tags:
      - WORKBENCH
      - OPTIONAL
    description: An intra-city bus booking workflow where passengers search available transit services, select their preferred route, and confirm their reservation in a direct transaction sequence without post-confirmation status updates.
    config:
      $ref: ./Bus/IntraCity_Vehicle_Based_Confirmation_flow_Without_Update_Call_.yaml
  - type: playground
    id: DELAYED_CANCELLATION_FLOW_ACCEPTED_IN_CASE_OF_TECHNICAL_CANCELLATION
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A buyer searches for and confirms a metro ticket booking, which the seller accepts. The buyer then initiates a cancellation through the delayed cancellation flow, validating the system's ability to handle post-acceptance cancellation requests.
    config:
      $ref: ./Metro/DELAYED_CANCELLATION_FLOW_ACCEPTED.yaml
  - type: playground
    id: DELAYED_CANCELLATION_FLOW_ACCEPTED_IN_CASE_OF_TECHNICAL_CANCELLATION (W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A passenger searches for a metro route and confirms a ticket booking, which is subsequently cancelled due to technical constraints, with the system accepting and processing the delayed technical cancellation.
    config:
      $ref: ./Metro/DELAYED_CANCELLATION_FLOW_ACCEPTED__W_O_Search_and_Select_.yaml
  - type: playground
    id: DELAYED_CANCELLATION_FLOW_REJECTED_IN_CASE_OF_TECHNICAL_CANCELLATION
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Buyer completes a metro booking from search through confirmation, then attempts to cancel the confirmed order, but the platform rejects the cancellation request due to technical or operational constraints.
    config:
      $ref: ./Metro/DELAYED_CANCELLATION_FLOW_REJECTED.yaml
  - type: playground
    id: DELAYED_CANCELLATION_FLOW_REJECTED_IN_CASE_OF_TECHNICAL_CANCELLATION (W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A passenger books a metro journey and subsequently requests cancellation, but the cancellation request is delayed and rejected due to technical constraints or timing limitations that prevent the booking reversal.
    config:
      $ref: ./Metro/DELAYED_CANCELLATION_FLOW_REJECTED__W_O_Search_and_Select_.yaml
  - type: playground
    id: DELAYED_CANCELLATION_FLOW_ACCEPTED_IN_CASE_OF_USER_CANCELLATION
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A customer books a metro ticket and initiates a cancellation request after the booking has been confirmed and accepted, testing the delayed post-confirmation cancellation workflow.
    config:
      $ref: ./Metro/DELAYED_CANCELLATION_FLOW_ACCEPTED_IN_CASE_OF_USER_CANCELLATION.yaml
  - type: playground
    id: DELAYED_CANCELLATION_FLOW_ACCEPTED_IN_CASE_OF_USER_CANCELLATION (W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A passenger confirms a metro ticket booking and subsequently initiates a cancellation that the system accepts and processes.
    config:
      $ref: ./Metro/DELAYED_CANCELLATION_FLOW_ACCEPTED_IN_CASE_OF_USER_CANCELLATION__W_O_Search_and_Select_.yaml
  - type: playground
    id: DELAYED_CANCELLATION_FLOW_REJECTED_IN_CASE_OF_USER_CANCELLATION
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Buyer completes a metro ticket booking journey through search, selection, and confirmation, then attempts to cancel the confirmed order but receives a rejection due to business rules or operational constraints. This flow validates the platform's ability to enforce cancellation policies and communicate rejection to the buyer.
    config:
      $ref: ./Metro/DELAYED_CANCELLATION_FLOW_REJECTED_IN_CASE_OF_USER_CANCELLATION.yaml
  - type: playground
    id: DELAYED_CANCELLATION_FLOW_REJECTED_IN_CASE_OF_USER_CANCELLATION (W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A passenger completes a metro ticket booking and subsequently requests cancellation, but the operator rejects the cancellation request due to timing or business constraints.
    config:
      $ref: ./Metro/DELAYED_CANCELLATION_FLOW_REJECTED_IN_CASE_OF_USER_CANCELLATION__W_O_Search_and_Select_.yaml
  - type: playground
    id: END_STOP_UPDATE_FLOW
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Customer searches for available metro routes, selects a preferred service to their destination, confirms booking, and receives multiple real-time status updates tracking journey progress throughout the complete trip lifecycle.
    config:
      $ref: ./Metro/END_STOP_UPDATE_FLOW.yaml
  - type: playground
    id: END_STOP_UPDATE_FLOW (W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Enables metro passengers to search for transit routes, initiate a ticket booking with an initial destination, and adjust their alighting stop multiple times during the transaction before completing payment.
    config:
      $ref: ./Metro/END_STOP_UPDATE_FLOW__W_O_Search_and_Select_.yaml
  - type: playground
    id: STATION_CODE_FLOW_CATALOG
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A commuter searches for metro stations using a station code to discover available transit services and station information.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_CATALOG.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A commuter searches for metro connections using station codes, selects a preferred service, confirms the booking, and tracks order status in real-time.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER (W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A metro passenger searches for and books a journey between stations using station codes, proceeding directly from search discovery to booking confirmation without an intermediate service selection step.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER__W_O_Search_and_Select_.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER_IGM(v-2.0.0)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A customer books metro tickets by searching with a station code and completing their order, with integrated capabilities to file, track, and resolve grievances throughout the transaction lifecycle.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER_IGM_v-2_0_0_.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER_IGM(v-2.0.0)(W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A customer purchases a metro ticket using station code search, confirms the booking, tracks order status, and resolves transaction issues through the integrated grievance management system.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER_IGM_v-2_0_0__W_O_Search_and_Select_.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER_IGM_No_Action(v-1.0.0)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Customers search for and book metro journeys using station codes, with any transaction issues automatically detected and resolved without requiring customer intervention.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER_IGM_No_Action_v-1_0_0_.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER_IGM_No_Action(v-1.0.0)(W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A metro passenger discovers and books a ticket using station codes, then reports an unresolved transaction issue and tracks its resolution through the platform's issue management system.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER_IGM_No_Action_v-1_0_0__W_O_Search_and_Select_.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER_IGM_No_Action(v-2.0.0)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A customer searches and books metro tickets using station codes, then escalates service grievances through repeated issue management cycles when the provider fails to take corrective action.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER_IGM_No_Action_v-2_0_0_.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER_IGM_No_Action(v-2.0.0)(W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: End-to-end metro transit ticketing transaction where customers search routes using station codes, complete ticket booking and confirmation, track order fulfillment status, and escalate service issues through integrated grievance management.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER_IGM_No_Action_v-2_0_0__W_O_Search_and_Select_.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER_IGM_Rejection(v-2.0.0)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A metro passenger searches and books a ticket using station codes, then files a complaint that is rejected through the ONDC issue resolution process.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER_IGM_Rejection_v-2_0_0_.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER_IGM_Rejection(v-2.0.0)(W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A passenger searches for and confirms a metro ticket booking using station code, but the order is rejected, requiring resolution through multiple interactions with the issue and grievance management system.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER_IGM_Rejection_v-2_0_0__W_O_Search_and_Select_.yaml
  - type: playground
    id: TECHNICAL_CANCELLATION_FLOW
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A customer completes an end-to-end metro booking on ONDC through discovery and selection, confirms the transaction, and then cancels the reservation to validate the full technical cancellation workflow and transaction reversal process.
    config:
      $ref: ./Metro/TECHNICAL_CANCELLATION_FLOW.yaml
  - type: playground
    id: TECHNICAL_CANCELLATION_FLOW (W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A passenger discovers and confirms a metro ticket booking through the platform, then exercises a technical cancellation to reverse the transaction, validating the complete order lifecycle from ticket discovery through confirmed booking to successful reversal.
    config:
      $ref: ./Metro/TECHNICAL_CANCELLATION_FLOW__W_O_Search_and_Select_.yaml
  - type: playground
    id: USER_CANCELLATION_FLOW
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A passenger discovers and selects available metro transit services, confirms the booking, and then cancels the reservation, demonstrating the complete end-to-end cancellation workflow in metro ticketing operations.
    config:
      $ref: ./Metro/USER_CANCELLATION_FLOW.yaml
  - type: playground
    id: USER_CANCELLATION_FLOW (W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A metro passenger searches for transit services, confirms a booking, and then cancels the reservation to validate the end-to-end cancellation workflow on the ONDC platform. This flow ensures proper handling of reservation cancellations in metro transportation transactions.
    config:
      $ref: ./Metro/USER_CANCELLATION_FLOW__W_O_Search_and_Select_.yaml
  - type: playground
    id: PARTIAL_CANCELLATION_FLOW
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A customer searches for available metro routes, completes a ticket booking, and subsequently initiates a partial cancellation while the system processes applicable refunds and maintains updated transaction status.
    config:
      $ref: ./Metro/PARTIAL_CANCELLATION_FLOW.yaml
  - type: playground
    id: PARTIAL_CANCELLATION_FLOW (W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A customer searches for metro routes, confirms a booking, and subsequently initiates a partial cancellation, with the system applying cancellation policies and updating the order status.
    config:
      $ref: ./Metro/PARTIAL_CANCELLATION_FLOW_W_O_Search_and_Select.yaml
  - type: playground
    id: OFFLINE_CANCELLATION_FLOW
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A customer books a metro transit service through the ONDC platform and subsequently cancels the reservation offline; the system detects and processes the offline cancellation, updates the order status, and settles any applicable refunds or charges.
    config:
      $ref: ./Metro/OFFLINE_CANCELLATION_FLOW.yaml
  - type: playground
    id: OFFLINE_CANCELLATION_FLOW (W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A customer searches for, selects, and confirms a metro transit booking, then cancels the transaction through offline request. The system processes the cancellation with order status updates and refund settlement.
    config:
      $ref: ./Metro/OFFLINE_CANCELLATION_FLOW_W_O_Search_and_Select.yaml
