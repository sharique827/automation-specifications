flows:
  - type: playground
    id: IntraCity_Merchant_Side_Cancellation
    usecase: Bus
    tags:
      - PRAMAAN
      - MANDATORY
      - REPORTABLE
      - WORKBENCH
    description: Intra-city bus booking journey from ticket discovery through confirmation, followed by merchant-initiated cancellation of the confirmed reservation.
    config:
      $ref: ./Bus/IntraCity_Merchant_Side_Cancellation.yaml
  - type: playground
    id: IntraCity_Monthly_Passes_Flow_Code_Based
    usecase: Bus
    tags:
      - PRAMAAN
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Enables a commuter to discover, select, and purchase a monthly unlimited intra-city bus pass, with digital code-based access for seamless transit entry.
    config:
      $ref: ./Bus/IntraCity_Monthly_Passes_Flow_Code_Based.yaml
  - type: playground
    id: IntraCity_Purchase_Journey_FlowCode_Based_Flow_With_IGM(v-1.0.0)
    usecase: Bus
    tags:
      - MANDATORY
      - WORKBENCH
    description: End-to-end intra-city bus ticket booking and issue management flow, from passenger search through post-purchase support and complaint resolution.
    config:
      $ref: ./Bus/IntraCity_Purchase_Journey_FlowCode_Based_Flow_With_IGM_v-1_0_0_.yaml
  - type: playground
    id: IntraCity_Purchase_Journey_FlowCode_Based_Flow_With_IGM(v-2.0.0)
    usecase: Bus
    tags:
      - MANDATORY
      - WORKBENCH
    description: Intra-city bus ticket purchase journey combining service discovery, selection, and booking confirmation with integrated issue and grievance management capabilities for complete customer support.
    config:
      $ref: ./Bus/IntraCity_Purchase_Journey_FlowCode_Based_Flow_With_IGM_v-2_0_0_.yaml
  - type: playground
    id: IntraCity_Purchase_Journey_FlowCode_Based_Flow_With_IGM_No_Action(v-2.0.0)
    usecase: Bus
    tags:
      - MANDATORY
      - WORKBENCH
    description: Complete intra-city bus ticket purchase journey from service discovery and selection through order confirmation, including post-purchase issue grievance management.
    config:
      $ref: ./Bus/IntraCity_Purchase_Journey_FlowCode_Based_Flow_With_IGM_No_Action_v-2_0_0_.yaml
  - type: playground
    id: IntraCity_Purchase_Journey_FlowCode_Based_Flow_With_IGM_Rejection(v-2.0.0)
    usecase: Bus
    tags:
      - MANDATORY
      - WORKBENCH
    description: Intra-city bus ticket purchase journey encompassing search, selection, booking confirmation, and comprehensive issue and grievance management including rejection scenarios.
    config:
      $ref: ./Bus/IntraCity_Purchase_Journey_FlowCode_Based_Flow_With_IGM_Rejection_v-2_0_0_.yaml
  - type: playground
    id: IntraCity_Purchase_Journey_Flow_Code_Based
    usecase: Bus
    tags:
      - PRAMAAN
      - MANDATORY
      - REPORTABLE
      - WORKBENCH
    description: Enables end-to-end intra-city bus ticket purchasing, from route discovery through booking confirmation and real-time order tracking.
    config:
      $ref: ./Bus/IntraCity_Purchase_Journey_Flow_Code_Based.yaml
  - type: playground
    id: IntraCity_Search_Pagination_FlowCode_Based
    usecase: Bus
    tags:
      - WORKBENCH
      - OPTIONAL
    description: A buyer searches for available bus services within a city, and the seller returns matching results in paginated responses.
    config:
      $ref: ./Bus/IntraCity_Search_Pagination_FlowCode_Based.yaml
  - type: playground
    id: IntraCity_Technical_Cancellation_Flow
    usecase: Bus
    tags:
      - PRAMAAN
      - MANDATORY
      - REPORTABLE
      - WORKBENCH
    description: Complete end-to-end transaction for intra-city bus ticket booking from search and selection through confirmation and technical cancellation processing.
    config:
      $ref: ./Bus/IntraCity_Technical_Cancellation_Flow.yaml
  - type: playground
    id: IntraCity_User_Based_Confirmation_flow
    usecase: Bus
    tags:
      - WORKBENCH
      - OPTIONAL
    description: End-to-end intra-city bus booking where users search for available routes, select a preferred service, and explicitly confirm their reservation before the transaction is finalized.
    config:
      $ref: ./Bus/IntraCity_User_Based_Confirmation_flow.yaml
  - type: playground
    id: IntraCity_User_Cancellation_Flow
    usecase: Bus
    tags:
      - PRAMAAN
      - MANDATORY
      - REPORTABLE
      - WORKBENCH
    description: Demonstrates user cancellation of a confirmed intra-city bus ticket through the complete booking and cancellation lifecycle. Validates the end-to-end flow from passenger search and selection through booking confirmation and final cancellation acknowledgment.
    config:
      $ref: ./Bus/IntraCity_User_Cancellation_Flow.yaml
  - type: playground
    id: IntraCity_Vehicle_Based_Confirmation_flow(With Update Call)
    usecase: Bus
    tags:
      - WORKBENCH
      - OPTIONAL
    description: An intra-city bus booking flow that guides passengers through searching for services, selecting a bus, confirming their reservation, and updating booking details.
    config:
      $ref: ./Bus/IntraCity_Vehicle_Based_Confirmation_flow_With_Update_Call_.yaml
  - type: playground
    id: IntraCity_Vehicle_Based_Confirmation_flow(Without Update Call)
    usecase: Bus
    tags:
      - WORKBENCH
      - OPTIONAL
    description: A complete intra-city bus reservation workflow where passengers search available services, select their preferred option, and finalize their booking through confirmation.
    config:
      $ref: ./Bus/IntraCity_Vehicle_Based_Confirmation_flow_Without_Update_Call_.yaml
  - type: playground
    id: DELAYED_CANCELLATION_FLOW_ACCEPTED
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: User searches for and selects a metro service, then initiates and confirms the booking. After the booking is accepted, the user cancels the reservation through the delayed cancellation flow.
    config:
      $ref: ./Metro/DELAYED_CANCELLATION_FLOW_ACCEPTED.yaml
  - type: playground
    id: DELAYED_CANCELLATION_FLOW_ACCEPTED (W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A passenger searches for a metro route, confirms a ticket booking, and then successfully cancels the confirmed reservation.
    config:
      $ref: ./Metro/DELAYED_CANCELLATION_FLOW_ACCEPTED__W_O_Search_and_Select_.yaml
  - type: playground
    id: DELAYED_CANCELLATION_FLOW_REJECTED
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Buyer initiates cancellation of a confirmed metro booking after selection and confirmation, but the platform rejects the cancellation request due to business rules or operational constraints.
    config:
      $ref: ./Metro/DELAYED_CANCELLATION_FLOW_REJECTED.yaml
  - type: playground
    id: DELAYED_CANCELLATION_FLOW_REJECTED (W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: User requests cancellation of a confirmed metro booking, but the cancellation request is delayed and ultimately rejected due to timing or business constraints.
    config:
      $ref: ./Metro/DELAYED_CANCELLATION_FLOW_REJECTED__W_O_Search_and_Select_.yaml
  - type: playground
    id: END_STOP_UPDATE_FLOW
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Customer searches for available metro routes, selects a preferred service to their destination, completes booking confirmation, and receives multiple journey status updates throughout the transaction lifecycle.
    config:
      $ref: ./Metro/END_STOP_UPDATE_FLOW.yaml
  - type: playground
    id: END_STOP_UPDATE_FLOW (W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Allows a metro passenger to search for transit routes, initiate a booking, and update their destination stop multiple times during the transaction before confirming and completing the payment, streamlining the ticket purchase flow without a separate selection confirmation step.
    config:
      $ref: ./Metro/END_STOP_UPDATE_FLOW__W_O_Search_and_Select_.yaml
  - type: playground
    id: STATION_CODE_FLOW_CATALOG
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A commuter searches for metro stations using a station code to discover transit services and station-specific information within the network.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_CATALOG.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: User searches and books a metro ticket using station codes, selects their preferred service, confirms the order, and tracks order status.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER (W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A metro passenger searches for and books a journey between stations using station codes, proceeding directly from search discovery through booking confirmation without an intermediate service selection step.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER__W_O_Search_and_Select_.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER_IGM(v-2.0.0)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Customer uses a station code to search for and purchase metro tickets, completes the order, and can file and manage grievances if issues arise during the transaction.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER_IGM_v-2_0_0_.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER_IGM(v-2.0.0)(W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A customer books a metro ticket using station code search, confirms the booking, tracks order status, and resolves any transaction issues through the integrated grievance management process.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER_IGM_v-2_0_0__W_O_Search_and_Select_.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER_IGM_No_Action(v-1.0.0)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Customers search and book metro journeys using station codes, with order issues automatically resolved without requiring further customer action.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER_IGM_No_Action_v-1_0_0_.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER_IGM_No_Action(v-1.0.0)(W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A passenger searches for metro stations using station codes, confirms a ticket booking, and submits an unresolved issue to validate the issue management workflow.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER_IGM_No_Action_v-1_0_0__W_O_Search_and_Select_.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER_IGM_No_Action(v-2.0.0)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A customer searches and books metro tickets using station codes, then escalates service issues when the provider takes no corrective action.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER_IGM_No_Action_v-2_0_0_.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER_IGM_No_Action(v-2.0.0)(W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: End-to-end metro ticketing transaction where customers search for transit routes using station codes, complete booking and order confirmation, track shipment status, and escalate service issues through automated grievance management.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER_IGM_No_Action_v-2_0_0__W_O_Search_and_Select_.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER_IGM_Rejection(v-2.0.0)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: Customer searches metro stations using station codes, completes a ticket booking, and navigates through the full issue resolution workflow when their complaint is rejected.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER_IGM_Rejection_v-2_0_0_.yaml
  - type: playground
    id: STATION_CODE_FLOW_ORDER_IGM_Rejection(v-2.0.0)(W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A passenger searches for metro tickets by station code, confirms the booking, then processes an order rejection through the issue and grievance management system.
    config:
      $ref: ./Metro/STATION_CODE_FLOW_ORDER_IGM_Rejection_v-2_0_0__W_O_Search_and_Select_.yaml
  - type: playground
    id: TECHNICAL_CANCELLATION_FLOW
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A customer discovers and books a metro transportation service through ONDC, then cancels the reservation to verify the complete technical cancellation workflow from booking to transaction reversal.
    config:
      $ref: ./Metro/TECHNICAL_CANCELLATION_FLOW.yaml
  - type: playground
    id: TECHNICAL_CANCELLATION_FLOW (W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A passenger searches for metro tickets, confirms a booking, and then cancels the confirmed order. The flow validates the technical cancellation workflow from ticket discovery through transaction reversal.
    config:
      $ref: ./Metro/TECHNICAL_CANCELLATION_FLOW__W_O_Search_and_Select_.yaml
  - type: playground
    id: USER_CANCELLATION_FLOW
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A passenger discovers and selects from available metro services, confirms a booking, and then cancels the reservation, validating the complete cancellation workflow from service discovery through successful cancellation.
    config:
      $ref: ./Metro/USER_CANCELLATION_FLOW.yaml
  - type: playground
    id: USER_CANCELLATION_FLOW (W/O Search1 and Select)
    usecase: Metro
    tags:
      - WORKBENCH
      - MANDATORY
      - REPORTABLE
    description: A user searches for metro services, initiates and confirms a booking, and then cancels the confirmed reservation. This flow validates the end-to-end cancellation experience for metro transit transactions on the ONDC network.
    config:
      $ref: ./Metro/USER_CANCELLATION_FLOW__W_O_Search_and_Select_.yaml
