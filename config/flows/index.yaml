flows:
  - type: playground
    id: RECONCILIATION
    usecase: RECONCILIATION
    tags:
      - WORKBENCH
      - PRAMAAN
      - MANDATORY
      - REPORTABLE
    description: Recon Flow
    config:
      $ref: ./RECONCILIATION/Recon_Flow.yaml
